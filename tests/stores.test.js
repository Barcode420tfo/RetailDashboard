import test from 'node:test';import assert from 'node:assert/strict';import {buildStoreReporting} from '../server/services/stores.js';
test('store reporting preserves totals, groups whitespace/case within zone and leaves ambiguous links pending',()=>{const clusters=[{_id:'a',zone:'East',name:'East 1'},{_id:'b',zone:'West',name:'West 1'}];const stores=[{storeId:'1',name:'Shop',cluster:'a'},{storeId:'2',name:'Shop',cluster:'a'},{storeId:'3',name:'Shop',cluster:'b'}];const rows=[{sourceStoreName:' Shop ',sourceZone:'East',valueKobo:10000,businessDate:'2026-09-01'},{sourceStoreName:'shop',sourceZone:'East',valueKobo:20000,businessDate:'2026-09-02'},{sourceStoreName:'Shop',sourceZone:'West',valueKobo:5000,businessDate:'2026-09-02'}];const d=buildStoreReporting({rows,stores,clusters});assert.equal(d.performance.length,2);assert.equal(d.performance.reduce((a,r)=>a+r.value,0),350);assert.equal(d.performance[0].matchStatus,'Multiple master candidates');assert.equal(d.performance[0].id,null);assert.equal(d.performance[0].daysSold,2);assert.equal(d.performance[1].id,'3');assert.equal(buildStoreReporting({rows:[],stores,clusters,zone:'West'}).directory.length,1);});
